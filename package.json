{
  "name": "gesturecam",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "GestureCam - 手势控制口播素材相机",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1"
  }
}
