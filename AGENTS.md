# GestureCam — 给 AI 会话的工作约定

这个项目是**长会话**开发的（摄像头 + 手势，靠真机反馈迭代），所以每条约定都围绕
"别在无依赖的往返上重复付费"。详细背景见 `docs/DECISIONS.md` 第 22 节。

## 开场先做一件事

**动手改代码或跑验证之前，先 `skill` 加载 `token-discipline`。**
那里面有分级验证、禁止猜着调试、读文件与汇报的具体做法（本文件的六条是它的简版）。

## 六条硬规矩

1. **验证分级，别每次全量。**
   - 小改动：`node_modules\.bin\tsc.cmd --noEmit` + **相关**测试文件
     （`vitest run tests/flick.test.ts`）
   - 改了公共模块：`node_modules\.bin\vitest.cmd run`
   - 浏览器验收（4 分钟 / 76 项）：**一个任务只跑一次，放在最后**。
     它包含真实录制等慢用例，且 90% 的检查与本次改动无关。
2. **一次失败的假设之后必须停下来**，把原始输出/错误原文打出来看；
   不许接着试第二、第三种猜想。"猜 → 验证 → 猜错 → 再猜"是最贵的循环。
3. **读文件带 `offset`/`limit`**：先 `grep` 定位行号，再只读那一段。
4. **无依赖的调用合并到同一批**（读+读、改+改），不要串行往返。
5. **回答要短**：结论 + 证据 + 下一步，不重复 `docs/` 里已经写好的内容
   （回答会进入下一轮历史并被反复计费）。
6. **一次性诊断脚本用完即删**，不要留在 `tools/` 里当资产。

## 项目硬约束（会踩坑的地方）

- **只能在 `edit`/`write` 工具里编辑文件。** 不要用 PowerShell 拼接字符串改文件 ——
  中文 + 引号 + 反引号在 PowerShell 5.1 下会毁文件（`README.md` 被毁过一次）。
- **不要在 `verify-browser.mjs` 的探针里用嵌套模板字符串**（反引号套反引号），
  已经因此踩坑两次；探针里用字符串拼接。
- 合成输入必须**按真实时间喂帧**（rAF ≈16ms），不要把"帧数"当时间用。
- 改了手势阈值后，`tools/verify-browser.mjs` 里那些硬编码的期望值要同步更新。

## 验证入口

| 目的 | 命令 |
| --- | --- |
| 类型检查 | `node_modules\.bin\tsc.cmd --noEmit` |
| 单元测试（344 个） | `node_modules\.bin\vitest.cmd run` |
| 浏览器验收（76 项，慢） | `node tools/verify-browser.mjs` |
| 构建 | `node_modules\.bin\vite.cmd build` |

## 文档地图

| 文件 | 内容 |
| --- | --- |
| `docs/手势文法.md` | 手势含义、阈值表、标定记录（改阈值必须同步这里） |
| `docs/DECISIONS.md` | 每个决策的取舍与踩过的坑（含第 22 节的效率规矩） |
| `docs/需求文档-原文.txt` / `docs/参考指南-原文.txt` | 用户给的两份原始规格 |
| `README.md` | 项目全貌、阶段进度 |

## 交付习惯

调试叠层（画布左上角）是这个项目的**主要标定界面**：改手势相关的东西时，
要保证它把"读数 vs 门槛"显示得能让人一眼读出是哪一条在挡。
相关截图（`tools/*.png`）是给人过目用的验收物，断言绿了不等于东西能用。
